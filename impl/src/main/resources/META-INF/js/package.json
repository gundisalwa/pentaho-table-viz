{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "description": "${project.description}",

  "main": "main.js",

  "packages": [
    "tableviz"
  ],

  "config": {
  },

  "dependencies": {
    "underscorejs": "${underscore.version}",
    "jquery": "${jquery.version}",
    "datatables": "${datatables.version}"
  }
}
